import { Component, inject } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { CustomerAccountDataApiService } from '../../core/customer/customer-account-data-api.service';

@Component({ standalone: true, imports: [RouterLink], styleUrls: ['../auth-page.scss'], template: `<div class="auth-page"><section><div class="eyebrow">Account / Email</div><h1>Confirm your new address.</h1><p class="lede">For security, you will sign in again with the new email after confirmation.</p></section><section class="auth-panel">@if(loading){<h2>Confirming…</h2>}@if(success){<h2>Email changed</h2><p class="form-success">Your new email is verified. Please sign in again.</p>}@if(error){<h2>Confirmation failed</h2><p class="form-error">{{error}}</p>}<div class="auth-links"><a routerLink="/auth/login">Continue to sign in</a></div></section></div>` })
export class ConfirmEmailChangePage { private readonly route=inject(ActivatedRoute); private readonly api=inject(CustomerAccountDataApiService); loading=true;success=false;error=''; constructor(){const token=this.route.snapshot.queryParamMap.get('token');if(!token){this.loading=false;this.error='Missing confirmation token.';return;}this.api.confirmEmailChange(token).subscribe({next:()=>{this.loading=false;this.success=true;},error:()=>{this.loading=false;this.error='This link is invalid, expired, or the email is no longer available.';}});} }
